name: hola-mundo
on: [push]
jobs:
  hola-mundo-compile:
    runs-on: ubuntu-latest
    steps: 
    - name: Checkout
      uses: actions/checkout@v2
    - name: Definir una variable de entorno
      run: echo "HOLA_MUNDO=Hola Mundo" >> $GITHUB_ENV
    - name: Run hola-mundo
      run: php index.php
